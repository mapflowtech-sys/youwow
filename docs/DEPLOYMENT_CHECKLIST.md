# 🚀 Deployment Checklist - 1plat Payment Integration

## ✅ Pre-Deployment Verification

### Local Testing ✓
- [x] TypeScript compilation - No errors
- [x] Build process - Successfully completed
- [x] Development server - Running on localhost:3000
- [x] /song page - Loads without errors
- [x] Payment modal - Renders correctly
- [ ] **Ручное тестирование**: Откройте http://localhost:3000/song и проверьте:
  - Форма заполняется корректно
  - Кнопка "Получить готовую песню" открывает модальное окно
  - Модальное окно оплаты показывает правильную цену (590₽)
  - Выбор метода оплаты (карта/СБП) работает

### Environment Variables
Проверьте `.env.local`:
```bash
# Payment Configuration
ONEPLAT_SHOP_ID=1296
ONEPLAT_SECRET=Kogortalove1!

# Application URL (ВАЖНО: Обновить для production!)
NEXT_PUBLIC_APP_URL=http://localhost:3000  # ← Изменить на https://yourdomain.com
```

## 🔧 Configuration Steps for 1plat

### 1. Webhook Configuration
В личном кабинете 1plat установите:

**Webhook URL:**
```
https://yourdomain.com/api/payment/1plat/webhook
```

**Метод:** POST

**Что происходит:**
- 1plat отправляет уведомление при изменении статуса платежа
- Webhook проверяет подпись
- При успешной оплате автоматически запускается генерация песни

### 2. Success URL
```
https://yourdomain.com/payment/success
```

**Что происходит:**
- Пользователь перенаправляется сюда после успешной оплаты
- Через 5 секунд автоматически перенаправляется на `/order/[id]`
- Показывается статус генерации песни

### 3. Failure URL
```
https://yourdomain.com/payment/failure
```

**Что происходит:**
- Пользователь перенаправляется сюда при ошибке оплаты
- Показываются возможные причины
- Кнопка "Попробовать снова" возвращает на /song

### 4. Verification File
Файл уже создан: `public/1plat.txt` с содержимым `1296`

**Проверьте доступность:**
```
https://yourdomain.com/1plat.txt
```

### 5. Enable 2FA (Рекомендуется!)
В разделе "Безопасность" личного кабинета 1plat:
- Включите двухфакторную аутентификацию
- Используйте Google Authenticator или Authy
- Это защитит аккаунт с реальными деньгами

## 📋 Deployment Process

**Важно:** Проект развернут на **TimeWeb Cloud** (не Vercel), используется Docker.

### 1. Update Environment Variables

В TimeWeb Cloud установите переменные окружения:

```bash
ONEPLAT_SHOP_ID=1296
ONEPLAT_SECRET=Kogortalove1!
NEXT_PUBLIC_APP_URL=https://yourdomain.com  # ← Ваш реальный домен!

# Другие существующие переменные
SUPABASE_URL=...
SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
GEN_API_KEY=...
RESEND_API_KEY=...
```

### 2. Deploy to Production

```bash
# Убедитесь, что все изменения закоммичены
git status

# Добавьте файлы
git add .

# Создайте коммит
git commit -m "feat: Add 1plat payment integration"

# Отправьте на GitHub
git push origin main
```

### 3. Configure 1plat URLs (ПОСЛЕ деплоя!)

⚠️ **ВАЖНО**: Настройте URLs в 1plat ТОЛЬКО после того, как сайт будет доступен в продакшене!

1. Зайдите в личный кабинет 1plat
2. Перейдите в настройки магазина
3. Установите URLs (см. раздел "Configuration Steps" выше)
4. Сохраните изменения

### 4. Test Payment Flow in Production

1. **Создайте тестовый заказ:**
   - Перейдите на https://youwow.ru/song
   - Заполните форму
   - Нажмите "Получить готовую песню"

2. **Проверьте модальное окно:**
   - Корректная цена (50₽ для теста, затем 590₽)
   - Выбор метода оплаты работает
   - Кнопка "Перейти к оплате" перенаправляет на 1plat

3. **Выполните тестовую оплату:**
   - Сейчас стоит тестовая цена 50₽
   - После успешного теста вернуть цену на 590₽ в `lib/payment/config.ts`
   - Проверьте перенаправление на success page
   - Убедитесь, что генерация запускается

4. **Проверьте webhook:**
   - Зайдите в логи TimeWeb Cloud
   - Найдите логи с префиксом `[1plat Webhook]`
   - Убедитесь, что webhook получен и обработан

5. **Проверьте базу данных:**
   - Supabase → Table Editor → orders
   - Найдите ваш заказ
   - Проверьте, что `status = 'paid'` или `'processing'`
   - Проверьте, что `payment_id` заполнен

## 🔍 Troubleshooting

### Webhook не приходит
1. Проверьте URL в настройках 1plat
2. Убедитесь, что домен доступен из интернета
3. Проверьте Vercel Logs на наличие ошибок
4. Убедитесь, что файл `/api/payment/1plat/webhook/route.ts` существует

### Генерация не запускается
1. Проверьте логи webhook: `[1plat Webhook] Order marked as paid`
2. Проверьте, что заказ найден в БД по `payment_id`
3. Проверьте, что API `/api/song/process` работает
4. Убедитесь, что `NEXT_PUBLIC_APP_URL` установлен правильно

### Invalid signature
1. Проверьте `ONEPLAT_SECRET` в переменных окружения
2. Убедитесь, что используете правильный ключ из ЛК 1plat
3. Проверьте, что нет лишних пробелов в ключе

### Payment creation fails
1. Проверьте `ONEPLAT_SHOP_ID` и `ONEPLAT_SECRET`
2. Проверьте логи: `[1plat] Payment creation failed`
3. Убедитесь, что магазин верифицирован в 1plat
4. Проверьте, что у магазина статус "Активен"

## 📊 Monitoring

### Где смотреть платежи:

1. **Личный кабинет 1plat:**
   - Раздел "Платежи"
   - История всех транзакций
   - Детали каждого платежа

2. **Supabase Dashboard:**
   ```sql
   SELECT
     id,
     created_at,
     customer_email,
     status,
     payment_id,
     amount
   FROM orders
   WHERE payment_provider = '1plat'
   ORDER BY created_at DESC
   LIMIT 50;
   ```

3. **TimeWeb Cloud Logs:**
   - Фильтр по `[Payment]` или `[1plat Webhook]`
   - Проверка успешности обработки

### Важные метрики:

- **Conversion Rate**: Оплаты / Открытия модального окна
- **Success Rate**: Успешные оплаты / Все попытки
- **Generation Rate**: Запущенные генерации / Успешные оплаты
- **Webhook Reliability**: Получено webhooks / Ожидаемо

## 🎯 Post-Deployment

### День 1
- [ ] Мониторить логи каждый час
- [ ] Проверить первые 5-10 платежей вручную
- [ ] Убедиться, что webhooks приходят стабильно

### Неделя 1
- [ ] Проверять ежедневно статистику в 1plat
- [ ] Мониторить ошибки в Vercel
- [ ] Собрать feedback от первых пользователей

### Долгосрочно
- [ ] Настроить алерты при ошибках webhook
- [ ] Создать дашборд для мониторинга платежей
- [ ] Добавить Free-Kassa как альтернативу

## 📝 Important Notes

1. **Цена**: Сейчас установлена 50₽ для ТЕСТОВОГО платежа
   - После успешного теста вернуть на 590₽
   - Изменить в `lib/payment/config.ts`
   - См. подробную инструкцию в `docs/HOW_TO_CHANGE_PRICE.md`

2. **Webhook Security**: Всегда проверяем подпись!
   - Никогда не отключайте проверку
   - При смене секретного ключа обновите `.env.local`

3. **User Experience**:
   - Модальное окно красивое и понятное
   - Все страницы (success/failure) хорошо оформлены
   - Автоматические редиректы работают

4. **Future Plans**:
   - Free-Kassa готов к добавлению
   - Email с чеком можно добавить позже
   - Статистика и аналитика

---

## 🎉 Ready for Production!

После выполнения всех пунктов выше, ваша платёжная система готова принимать реальные оплаты!

**Не забудьте:**
- ✅ Включить 2FA в 1plat
- ✅ Протестировать реальную оплату
- ✅ Проверить webhook в production
- ✅ Настроить мониторинг

**Good luck! 🚀**
