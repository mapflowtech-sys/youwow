МАНИФЕСТ ПРОЕКТА
YouWow — мультисервисная платформа персонализированных цифровых подарков на базе ИИ для российского рынка.
Миссия: WOW-эффект за 60 секунд. Пользователь заходит, выбирает сервис, оплачивает и получает уникальный контент.
Главные приоритеты:

Визуальный восторг (премиум дизайн)
Максимальная конверсия (каждый элемент продаёт)
Молниеносная разработка (2 дня на MVP)
Гибкость (легко добавлять опции и менять логику)

Целевая аудитория: Россия, Instagram/VK/TikTok трафик, возраст 18-45, импульсивные покупки 290-490₽.

📦 ОПИСАНИЕ СЕРВИСОВ
1. Гадание Таро (290₽)
Процесс:

Пользователь загружает фото лица (обязательно) + фото ладони (опционально, для юмора)
Заполняет: имя, дата рождения, тема гадания (любовь/деньги/карьера/сюрприз)
AI генерирует предсказание (смешное или мистическое)
AI генерирует карту Таро в уникальном стиле
FaceSwap: лицо пользователя вставляется в карту
Оплата ДО генерации картинки (чтобы не тратить деньги впустую)
Результат: картинка (карта с лицом) + текст предсказания
Отправка на email + доступ для скачивания на сайте

Расширяемость: В будущем добавятся новые тоны (грустное, романтическое), стили карт (классический, современный, киберпанк).

2. Видео от Деда Мороза (от 390₽)
Процесс:

Выбор: ребёнок или взрослый (разные сценарии)
Заполнение анкеты (имя, возраст, увлечения, достижения)
AI генерирует текст поздравления → показываем юзеру для одобрения
Оплата
ElevenLabs: текст → голос Деда Мороза
Sync.Labs: lip-sync (синхронизация губ с аудио на видео-шаблоне)
Результат: персональное видео
Отправка на email + скачивание на сайте

Расширяемость: В будущем добавятся новые персонажи (Эльф, Снеговик, Снегурочка), стили поздравлений, языки.

3. Персональная песня (490₽)
Процесс:

Заполнение: о ком песня, факты, стиль текста (прожарка/лирика/юмор), жанр музыки, голос (М/Ж)
AI генерирует текст песни → показываем юзеру
Превью 5 секунд (опционально)
Оплата
Suno: генерация полной песни
Результат: MP3 файл
Отправка на email + скачивание на сайте

Расширяемость: Новые жанры, длительность песни (30 сек / 1 мин / 3 мин), языки, эффекты.

🛠 ТЕХНОЛОГИЧЕСКИЙ СТЕК
Frontend

Next.js 14.2 (App Router) — для SEO, скорости, server components
TypeScript — strict mode, полная типизация
Tailwind CSS — утилитарные стили
Shadcn/UI — готовые доступные компоненты
Framer Motion — премиум анимации (fade, scale, page transitions)
Lucide React — современные SVG иконки
next-themes — переключатель светлой/тёмной темы

Backend & Database

Supabase — PostgreSQL + Auth (опционально) + Storage
Next.js API Routes — серверная логика

Платежи (универсальный адаптер)

Интерфейс: PaymentService (легко менять провайдеров)
Реализации: YooKassa, CloudPayments, Robokassa, Tinkoff
Принцип: Одна строка кода меняет провайдера

AI Services (универсальный адаптер)

Агрегатор: Eden AI (приоритет) — все AI в одном месте
Прямые API (если Eden AI не покрывает):

ElevenLabs — голос Деда Мороза
Sync.Labs — lip-sync видео
Suno — генерация песен
Replicate (InsightFace) — FaceSwap


Принцип: Можно менять провайдеров за 15 минут без переписывания кода

Storage

Supabase Storage — загруженные фото, готовые файлы (видео/песни/карты)

Email

Resend — отправка результатов (бесплатно до 3000/мес)

Deployment

Vercel — автодеплой из GitHub, edge functions

Version Control

GitHub — обязательные коммиты после каждого этапа

🎨 ДИЗАЙН-СИСТЕМА
Цветовая палитра
Светлая тема (по умолчанию):
colors: {
  primary: {
    DEFAULT: '#8b5cf6',  // Violet-500 (фиолетовый — магия, премиум)
    light: '#a78bfa',     // Violet-400
    dark: '#7c3aed',      // Violet-600
  },
  accent: {
    gold: '#f59e0b',      // Amber-500 (золотой — праздник, CTA)
    pink: '#ec4899',      // Pink-500 (акценты, важные элементы)
  },
  background: {
    DEFAULT: '#ffffff',
    secondary: '#f8fafc', // Slate-50
    card: '#f1f5f9',      // Slate-100
  },
  text: {
    primary: '#0f172a',   // Slate-900
    secondary: '#64748b', // Slate-500
    muted: '#94a3b8',     // Slate-400
  }
}

Тёмная тема:
dark: {
  primary: {
    DEFAULT: '#a78bfa',  // Violet-400 (ярче для тёмного фона)
    light: '#c4b5fd',
    dark: '#8b5cf6',
  },
  background: {
    DEFAULT: '#0f172a',  // Slate-950
    secondary: '#1e293b', // Slate-900
    card: '#334155',      // Slate-700
  },
  text: {
    primary: '#f8fafc',
    secondary: '#cbd5e1',
    muted: '#94a3b8',
  }
}

Типографика

Основной шрифт: Inter (Google Fonts) — 400, 500, 600, 700

Для всего текста (параграфы, описания, формы)


Акцентный шрифт: Space Grotesk (Google Fonts) — 600, 700

Для цен, заголовков, CTA кнопок



Размеры (Tailwind scale):

Мелкий текст: text-sm (14px)
Основной текст: text-base (16px)
Подзаголовки: text-lg (18px)
Заголовки H3: text-xl (20px)
Заголовки H2: text-2xl (24px)
Заголовки H1: text-4xl или text-5xl (36-48px)


🎯 ПРИНЦИПЫ ДИЗАЙНА (Конверсионный UX/UI)
1. Визуальная иерархия

Самое важное (CTA, цены) — самое большое и яркое
Заголовки: жирные (font-bold), контрастные
CTA кнопки: выделяются цветом accent-gold, крупные (min-h-12)
Вторичные элементы: приглушённые цвета (text-secondary)

2. Пространство (Whitespace)

Много воздуха между секциями (py-16 или py-24)
Не перегружать экран информацией
Группировка связанных элементов (space-y-4, gap-6)
Щедрые padding внутри карточек (p-6 или p-8)

3. Контраст и читаемость

Минимальный размер текста: 16px (text-base)
Контраст текста: WCAG AA стандарт (минимум 4.5:1)
Важные элементы: font-semibold или font-bold
Цвет ссылок: text-primary с подчёркиванием при hover

4. Движение и анимация

Цель: направлять взгляд к CTA, не отвлекать
Быстрые анимации: 200-300ms
Hover эффекты: scale (1.02), shadow, brightness
Page transitions: fade или slide (300ms)
Scroll animations: fade-in при появлении в viewport

5. Эмоциональный отклик

Эмодзи: умеренно (1-2 на секцию), к месту
Праздничная атмосфера: через цвета (фиолетовый + золотой)
Тексты: дружелюбные, не формальные

✅ "Создай магию за 60 секунд"
❌ "Воспользуйтесь нашими услугами"



6. Доверие и прозрачность

Цены: чёткие, без звёздочек, крупным шрифтом
Иконки платёжных систем в Footer (Мир, СБП, Visa/MC)
Формы: простые, понятные лейблы
Политика конфиденциальности: видимая ссылка
Время выполнения: "Готово за 10 минут" (управление ожиданиями)

7. Мобильная оптимизация (Mobile-First)

Сначала дизайн для 375px (iPhone SE)
Большие touch targets: минимум 48px (min-h-12)
Формы: крупные поля ввода, удобная клавиатура
Навигация: гамбургер-меню на мобиле
Модальные окна: fullscreen на мобиле


📱 АДАПТИВНОСТЬ (КРИТИЧНО)
Подход: Mobile-First + Universal Responsiveness
1. Сначала дизайним для мобильных устройств (375px)
2. Затем адаптируем для всех остальных размеров:

📱 Mobile S: 320px - 375px (старые iPhone)
📱 Mobile M: 375px - 640px (вертикальная ориентация)
📱 Mobile L: 640px - 768px (горизонтальная ориентация, фаблеты)
📱 Tablet: 768px - 1024px (iPad, планшеты)
💻 Desktop: 1024px - 1440px (ноутбуки)
🖥 Desktop XL: 1440px - 1920px (большие мониторы)
🖥 Desktop XXL: 1920px+ (4K мониторы)

Tailwind Breakpoints:
sm:   // от 640px
md:   // от 768px
lg:   // от 1024px
xl:   // от 1280px
2xl:  // от 1536px
```

### Правила адаптивности:

**❌ НЕ ДЕЛАЙ:**
- Фиксированные px размеры (`width: 500px`)
- Мелкий текст на мобиле (`text-xs`)
- Маленькие кнопки на touch-устройствах

**✅ ДЕЛАЙ:**
- Tailwind утилиты (`w-full`, `max-w-7xl`)
- Адаптивные размеры (`text-base md:text-lg lg:text-xl`)
- Большие touch targets (`min-h-12` на кнопках и полях)
- Тестируй на каждом breakpoint

### Особое внимание:

**Формы:**
- Поля ввода: `min-h-12` (48px) на всех устройствах
- Лейблы: `text-sm md:text-base`
- Spacing: `space-y-4 md:space-y-6`

**Кнопки:**
- Высота: `min-h-12` или `h-12` (48px для пальца)
- Ширина на мобиле: `w-full` (на всю ширину)
- Ширина на десктопе: `md:w-auto` (по контенту)

**Модальные окна:**
- Мобиле: fullscreen (`fixed inset-0`)
- Десктоп: centered (`max-w-lg mx-auto my-8`)

**Изображения:**
- Всегда `<Image>` компонент Next.js
- Атрибут `sizes`: правильные размеры для каждого breakpoint
- Оптимизация: WebP/AVIF форматы

---

## 🏗 АРХИТЕКТУРА КОДА

### Структура проекта (создаётся автоматически)
```
/app — страницы Next.js (App Router)
  /page.tsx — главная
  /(services)/tarot — сервис Таро
  /(services)/santa — сервис Деда Мороза
  /(services)/song — сервис Песни
  /order/[id] — страница заказа
  /legal — юридические страницы
  /api — бэкенд эндпоинты

/components
  /ui — Shadcn компоненты (button, input, card и т.д.)
  /shared — переиспользуемые (Header, Footer, ServiceCard, LoadingMagic)

/services
  /ai — универсальный AI провайдер + реализации
  /payment — универсальный платёжный сервис + реализации
  /email — отправка писем (Resend)
  /workflow — Workflow Engine (последовательность шагов)

/lib — утилиты, клиенты БД, валидация (Zod схемы)
/types — TypeScript типы и интерфейсы
/public — статика (видео-шаблоны, примеры)

🔧 УНИВЕРСАЛЬНЫЕ АДАПТЕРЫ (КРИТИЧНО)
1. AI Provider — легко менять AI сервисы
Проблема: Если захочешь сменить Eden AI на OpenAI — придётся переписывать весь код.
Решение: Универсальный интерфейс
// services/ai/ai-provider.ts

interface AIProvider {
  generateText(prompt: string, options?: any): Promise<string>;
  generateImage(prompt: string, style?: string): Promise<string>;
  generateVoice(text: string, voiceId: string): Promise<string>;
  generateSong(prompt: string, genre: string): Promise<string>;
  faceSwap(baseImage: string, faceImage: string): Promise<string>;
}

// Реализации
class EdenAIProvider implements AIProvider { ... }
class OpenAIProvider implements AIProvider { ... }
class ReplicateProvider implements AIProvider { ... }

// Переключение (ОДНА СТРОКА!)
const currentAI = new EdenAIProvider(); // ← меняешь только это
export const aiService = currentAI;
Результат: Смена провайдера = 1 строка кода, весь остальной код работает без изменений.

2. Payment Service — легко менять платёжки
Аналогично с платежами:
// services/payment/payment-service.ts

interface PaymentService {
  createPayment(order: Order): Promise<{ paymentUrl: string; paymentId: string }>;
  verifyWebhook(data: any): Promise<{ orderId: string; status: 'paid' | 'failed' }>;
}

// Реализации
class YooKassaProvider implements PaymentService { ... }
class CloudPaymentsProvider implements PaymentService { ... }

// Переключение (ОДНА СТРОКА!)
const currentPayment = new YooKassaProvider(); // ← меняешь только это
export const paymentService = currentPayment;

🗄 БАЗА ДАННЫХ (Supabase)
Таблица 1: users (минимальная идентификация)
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  total_orders INTEGER DEFAULT 0,
  last_order_at TIMESTAMP
);

CREATE INDEX idx_users_email ON users(email);

Назначение:

"Мягкая" идентификация по email (БЕЗ регистрации)
Все заказы пользователя связаны через email
В будущем легко добавить password_hash для полноценной регистрации

Таблица 2: orders (основная)
CREATE TABLE orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  created_at TIMESTAMP DEFAULT NOW(),
  
  -- Связь с пользователем
  user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  
  -- Тип сервиса
  service_type TEXT NOT NULL CHECK (service_type IN ('santa', 'tarot', 'song')),
  
  -- Статус обработки
  status TEXT NOT NULL DEFAULT 'pending' 
    CHECK (status IN ('pending', 'paid', 'processing', 'completed', 'failed')),
  
  -- Контакты клиента
  customer_email TEXT NOT NULL,
  customer_name TEXT,
  
  -- Входные данные (JSONB для гибкости)
  input_data JSONB NOT NULL,
  
  -- Результат
  result_url TEXT,
  result_metadata JSONB,
  
  -- Платёж
  payment_id TEXT,
  payment_provider TEXT,
  amount INTEGER NOT NULL,
  
  -- Техническая информация
  error_message TEXT,
  processing_started_at TIMESTAMP,
  completed_at TIMESTAMP
);

CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_email ON orders(customer_email);
CREATE INDEX idx_orders_payment_id ON orders(payment_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_service_type ON orders(service_type);

Примеры input_data (JSONB):
Таро:
json{
  "name": "Иван",
  "birthDate": "1990-05-15",
  "topic": "career",
  "tone": "mystical",
  "cardStyle": "classic",
  "photoUrl": "https://storage.supabase.co/.../photo.jpg",
  "palmPhotoUrl": "https://..." // опционально
}
Дед Мороз (ребёнок):
json{
  "recipientType": "child",
  "childName": "Маша",
  "childAge": 7,
  "hobby": "рисование",
  "achievement": "научилась кататься на коньках",
  "gift": "набор для творчества",
  "tone": "kind",
  "character": "santa"
}
Дед Мороз (взрослый):
json{
  "recipientType": "adult",
  "name": "Алексей",
  "relation": "friend",
  "tone": "humor",
  "character": "santa",
  "facts": "любит рыбалку, вечно опаздывает"
}
Песня:
json{
  "aboutPerson": "мой друг Алексей",
  "facts": "любит футбол и пиво, душа компании",
  "genre": "rap",
  "textStyle": "humor",
  "voice": "male",
  "duration": "60" // в секундах
}

Таблица 3: service_options (гибкость и расширяемость)
sqlCREATE TABLE service_options (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  service_type TEXT NOT NULL,     -- 'tarot', 'santa', 'song'
  option_key TEXT NOT NULL,       -- 'tone', 'character', 'genre', 'card_style'
  option_value TEXT NOT NULL,     -- 'funny', 'mystical', 'santa', 'rap'
  display_label TEXT NOT NULL,    -- "Весёлый сценарий", "Дед Мороз", "Рэп"
  description TEXT,               -- Дополнительное описание (опционально)
  price_modifier INTEGER DEFAULT 0, -- Доплата за опцию (например, +100₽)
  is_active BOOLEAN DEFAULT true,
  sort_order INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_service_options_type ON service_options(service_type);
CREATE INDEX idx_service_options_active ON service_options(is_active);
CREATE UNIQUE INDEX idx_service_options_unique ON service_options(service_type, option_key, option_value);
Примеры данных:
sql-- Таро: Тоны
INSERT INTO service_options (service_type, option_key, option_value, display_label, sort_order) VALUES
('tarot', 'tone', 'funny', 'Весёлое 😄', 1),
('tarot', 'tone', 'mystical', 'Мистическое 🔮', 2),
('tarot', 'tone', 'romantic', 'Романтическое 💕', 3),
('tarot', 'tone', 'dramatic', 'Драматичное 🎭', 4);

-- Таро: Стили карт
INSERT INTO service_options (service_type, option_key, option_value, display_label, sort_order) VALUES
('tarot', 'card_style', 'classic', 'Классический', 1),
('tarot', 'card_style', 'modern', 'Современный', 2),
('tarot', 'card_style', 'cyberpunk', 'Киберпанк 🤖', 3);

-- Дед Мороз: Персонажи
INSERT INTO service_options (service_type, option_key, option_value, display_label, price_modifier, sort_order) VALUES
('santa', 'character', 'santa', 'Дед Мороз 🎅', 0, 1),
('santa', 'character', 'elf', 'Эльф 🧝', 0, 2),
('santa', 'character', 'snowman', 'Снеговик ⛄', 50, 3);

-- Песня: Жанры
INSERT INTO service_options (service_type, option_key, option_value, display_label, sort_order) VALUES
('song', 'genre', 'pop', 'Поп 🎤', 1),
('song', 'genre', 'rap', 'Рэп 🎧', 2),
('song', 'genre', 'rock', 'Рок 🎸', 3),
('song', 'genre', 'chanson', 'Шансон 🎻', 4);
Как использовать в коде:
typescript// Автоматическая загрузка опций из БД
const toneOptions = await supabase
  .from('service_options')
  .select('*')
  .eq('service_type', 'tarot')
  .eq('option_key', 'tone')
  .eq('is_active', true)
  .order('sort_order');

// Теперь в форме динамически показываются все активные опции!

Преимущества:

✅ Добавить новую опцию = один INSERT (не меняя код)
✅ Отключить опцию = UPDATE is_active = false
✅ Изменить порядок = меняешь sort_order
✅ Установить доплату = price_modifier


⚙️ ГИБКИЙ WORKFLOW ENGINE
Проблема: Жёсткая последовательность шагов
Сейчас логика "зашита":форма → оплата → генерация → результат
Если захочешь поменять на: форма → превью → оплата → генерация — придётся переписывать код.
Решение: Конфигурируемые workflow
// services/workflow/workflow-engine.ts

type WorkflowStep = 
  | 'collect_data'     // Сбор данных из формы
  | 'generate_preview' // Генерация превью (текст, короткое аудио)
  | 'show_preview'     // Показ пользователю для одобрения
  | 'process_payment'  // Оплата
  | 'generate_full'    // Полная генерация (видео, песня, карта)
  | 'send_result'      // Отправка на email + показ на сайте
  | 'allow_download';  // Разрешить скачивание

interface ServiceWorkflow {
  serviceType: 'tarot' | 'santa' | 'song';
  steps: WorkflowStep[];
  config: {
    paymentBeforeGeneration: boolean;
    showPreview: boolean;
    allowPreviewEdit: boolean;
    sendEmail: boolean;
  };
}

// Конфигурация (можно хранить в БД или config файле)
const WORKFLOWS: Record<string, ServiceWorkflow> = {
  tarot: {
    serviceType: 'tarot',
    steps: [
      'collect_data',
      'generate_preview',  // Генерируем текст предсказания
      'show_preview',       // Показываем юзеру
      'process_payment',    // Оплата
      'generate_full',      // Генерируем карту с лицом
      'send_result',        // Email + показ
      'allow_download'
    ],
    config: {
      paymentBeforeGeneration: true,
      showPreview: true,
      allowPreviewEdit: false,
      sendEmail: true,
    }
  },
  
  santa: {
    serviceType: 'santa',
    steps: [
      'collect_data',
      'generate_preview',   // Генерируем текст поздравления
      'show_preview',
      'process_payment',
      'generate_full',      // Голос + видео
      'send_result',
      'allow_download'
    ],
    config: {
      paymentBeforeGeneration: true,
      showPreview: true,
      allowPreviewEdit: true, // Можно редактировать текст
      sendEmail: true,
    }
  },
  
  song: {
    serviceType: 'song',
    steps: [
      'collect_data',
      'generate_preview',   // Текст песни + 5 сек превью
      'show_preview',
      'process_payment',
      'generate_full',      // Полная песня
      'send_result',
      'allow_download'
    ],
    config: {
      paymentBeforeGeneration: true,
      showPreview: true,
      allowPreviewEdit: false,
      sendEmail: true,
    }
  }
};

Как менять логику:
Хочешь оплату ПОСЛЕ генерации?
steps: [
  'collect_data',
  'generate_preview',
  'show_preview',
  'generate_full',      // ← Генерация ДО оплаты
  'process_payment',    // ← Оплата ПОСЛЕ генерации
  'send_result',
  'allow_download'
],
config: { paymentBeforeGeneration: false }
Не нужен превью?
typescriptsteps: [
  'collect_data',
  'process_payment',
  'generate_full',
  'send_result',
  'allow_download'
]
Результат: Меняешь конфиг — меняется вся логика, код остаётся тот же.

📧 EMAIL — ОБЯЗАТЕЛЬНО
Правила отправки результатов
1. Email запрашивается в КАЖДОЙ форме:

Поле customer_email — required
Валидация: формат email (Zod: z.string().email())
Placeholder: "your@email.com"

2. После генерации — ВСЕГДА отправляем письмо:

Даже если пользователь скачал на сайте
Письмо = резервная копия + удобство

3. Содержание письма (через Resend):
Тема:

Таро: "🔮 Твоя карта Таро готова!"
Санта: "🎅 Видео от Деда Мороза ждёт тебя!"
Песня: "🎵 Твоя персональная песня готова!"

Тело письма (HTML):

Логотип YouWow
Заголовок: "Твой подарок готов! 🎉"
Превью результата (если картинка — встроить, если видео/аудио — скриншот)
Большая кнопка: "Скачать подарок" → ссылка на файл (действует 7 дней)
Альтернативная ссылка: "Посмотреть на сайте" → /order/[id]
Футер: "Создать ещё один подарок" → ссылка на главную

4. Также результат доступен на сайте:

Страница /order/[id]
Кнопка "Скачать" (прямая ссылка на файл в Supabase Storage)
Возможность поделиться (копировать ссылку)

5. Срок хранения файлов:

Supabase Storage: 30 дней (потом автоудаление)
В письме предупреждение: "Скачай в течение 30 дней"


⏳ АНИМАЦИЯ ОЖИДАНИЯ (LoadingMagic)
Проблема: Пользователь ждёт 10-60 секунд
AI генерация занимает время. Без обратной связи пользователь подумает, что сайт завис.
Решение: Компонент LoadingMagic
Требования:
1. Визуальная анимация (НЕ просто спиннер):

Таро: вращающиеся карты Таро или магический круг
Санта: падающий снег + анимированный Дед Мороз
Песня: пульсирующий эквалайзер (звуковые волны)

2. Динамический текст (меняется по стадиям):
typescriptconst LOADING_MESSAGES = {
  tarot: {
    generating: '🔮 Вселенная раскладывает карты...',
    processing: '✨ Звёзды шепчут предсказание...',
    finalizing: '🎨 Художник рисует твою карту...',
  },
  santa: {
    generating: '🎅 Дед Мороз пишет поздравление...',
    processing: '🎤 Записываем голос Деда Мороза...',
    finalizing: '🎬 Монтируем персональное видео...',
  },
  song: {
    generating: '🎵 Композитор сочиняет мелодию...',
    processing: '🎙 Вокалист записывает трек...',
    finalizing: '🎧 Сводим и мастерим песню...',
  }
};
```

**3. Прогресс-индикатор:**
- Если возможно определить стадию → прогресс-бар (0% → 100%)
- Если нет → пульсирующий индикатор

**4. Информация о времени:**
- "Обычно занимает 10-15 секунд"
- Таймер: "Прошло: 8 секунд"

**5. Кнопка отмены (опционально):**
- "Отменить заказ" (если пользователь передумал)
- Отмена → возврат средств (если уже оплатил)

**Технология:** Framer Motion (плавные transitions между стадиями)

---

## 🧭 НАВИГАЦИЯ И USER FLOW

### Принцип: Главная страница = хаб

**Структура навигации:**

1. **Точка входа: `/` (главная страница)**
   - Пользователь ВСЕГДА попадает сюда первым
   - Витрина сервисов: 3 большие карточки (Таро, Санта, Песня)
   - CTA: "Выбери свою магию ↓"

2. **Переходы на сервисы:**
   - Клик на карточку → `/tarot`, `/santa`, `/song`
   - URL понятные, SEO-дружественные

3. **Хлебные крошки (breadcrumbs):**
   - Всегда показываем путь: "Главная > Гадание Таро"
   - Кликабельные ссылки

4. **Header (постоянная навигация):**
   - Логотип (слева) → всегда ведёт на `/`
   - Кнопка "Мои заказы" (справа) → `/orders` (поиск по email)
   - Переключатель темы (светлая/тёмная)

5. **Footer (дополнительная навигация):**
   - Ссылки на все сервисы (Таро, Санта, Песня)
   - Юридические страницы (Политика, Оферта, Поддержка)

**Быстрый доступ:**
- Next.js `prefetch`: прелоад следующей страницы при hover
- Переходы: плавные (Framer Motion page transitions, 300ms)

**User Flow пример:**
```
Главная → Клик "Таро" → Форма → Заполнение → Превью текста → 
Оплата → Ожидание (LoadingMagic) → Результат → Скачивание/Email

📋 ПРАВИЛА РАЗРАБОТКИ
Безопасность
КРИТИЧНО:

❌ НИКОГДА не хардкодить API ключи в коде
✅ Всегда process.env.НАЗВАНИЕ_КЛЮЧА
✅ Создавать .env.example с заглушками (без реальных ключей)
❌ ЗАПРЕЩЕНО использовать localStorage/sessionStorage для API ключей

Исключение: Только для пользовательских настроек (тема, язык)



Файл .env.local (НЕ коммитим!):
envNEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
EDEN_AI_API_KEY=xxx
RESEND_API_KEY=re_xxx
YOOKASSA_SHOP_ID=xxx
YOOKASSA_SECRET_KEY=xxx
Файл .env.example (коммитим):
envNEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
EDEN_AI_API_KEY=your_eden_ai_key
RESEND_API_KEY=your_resend_key
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key

Код
Стиль:

Функциональные компоненты (не классы)
Async/await (не .then/catch цепочки)
TypeScript strict mode
Имена переменных: понятные, не сокращённые (user, orderData, а не u, od)

Комментарии:

Пиши комментарии только для нестандартной логики
Комментарии на русском языке
Пример:

typescript  // Проверяем webhook подпись от ЮKassa (защита от подделки)
  const isValid = verifyWebhookSignature(data, signature);
Импорты:

Группируй: сначала внешние библиотеки, потом свои модули
Используй alias @/ для корня проекта


Формы
Всегда:

React Hook Form + Zod валидация
Русские понятные сообщения об ошибках

✅ "Введите корректный email"
❌ "Invalid email format"



Обязательные поля:

Email: required + валидация формата
Чекбокс "Согласен с политикой конфиденциальности": required

Текст: "Я согласен с политикой конфиденциальности"
Ссылка на /legal/privacy



UX:

Показывать ошибки только после попытки submit (не при вводе)
Подсвечивать поле с ошибкой (красная рамка)
Фокус на первое поле с ошибкой

Платежи (КРИТИЧНО)
ЖЕЛЕЗНОЕ ПРАВИЛО:
✅ Генерация AI контента СТРОГО после статуса paid в БД
Процесс:

Пользователь заполняет форму → создаётся заказ со статусом pending
Переход на страницу оплаты (YooKassa/CloudPayments)
Пользователь оплачивает
Платёжка отправляет webhook на /api/payment/webhook
Проверяем подпись webhook (защита от подделки)
Обновляем статус заказа на paid
ТОЛЬКО ПОСЛЕ ЭТОГО запускаем AI генерацию
Обновляем статус на processing → completed

Защита:

Проверка подписи webhook (HMAC SHA-256)
Проверка суммы платежа (соответствует ли заказу)
Логирование всех webhook'ов в БД


Обработка ошибок
Всегда оборачивай внешние API в try-catch:
typescripttry {
  const result = await aiService.generateText(prompt);
  return result;
} catch (error) {
  console.error('AI generation failed:', error);
  
  // Сохраняем ошибку в БД
  await supabase
    .from('orders')
    .update({ 
      status: 'failed',
      error_message: error.message 
    })
    .eq('id', orderId);
  
  // Показываем пользователю понятное сообщение
  throw new Error('Что-то пошло не так. Попробуйте позже или напишите в поддержку.');
}
Пользователь видит:

Дружелюбное сообщение (не технические детали)
Кнопку "Написать в поддержку" (ссылка на email или Telegram)

Логи:

Детальные ошибки сохраняются в БД (поле error_message)
Для дебаггинга: console.error на сервере


SEO (с первого дня)
Каждая страница должна иметь:
1. Metadata (Next.js Metadata API):
typescriptexport const metadata = {
  title: 'Гадание Таро с твоим лицом | VibeGift',
  description: 'Узнай свою судьбу на 2026 год. ИИ создаст уникальную карту Таро с ТВОИМ лицом. Готово за 10 минут. От 290₽',
  openGraph: {
    title: 'Гадание Таро с твоим лицом',
    description: 'ИИ создаст карту Таро с твоим лицом за 10 минут',
    images: ['/og-tarot.jpg'],
  },
};
```

**2. Semantic HTML:**
- Используй правильные теги: `<h1>`, `<h2>`, `<article>`, `<section>`, `<nav>`
- Один `<h1>` на страницу
- Логическая иерархия заголовков (h1 → h2 → h3)

**3. Sitemap.xml:**
- Автогенерация в `/app/sitemap.ts`
- Включить все страницы: главная, сервисы, юридические

**4. Robots.txt:**
```
User-agent: *
Allow: /
Disallow: /api/
Disallow: /order/

Sitemap: https://vibegift.ru/sitemap.xml
5. Structured Data (JSON-LD):
typescriptconst productSchema = {
  "@context": "https://schema.org",
  "@type": "Product",
  "name": "Гадание Таро с твоим лицом",
  "description": "ИИ создаст карту Таро с вашим лицом",
  "offers": {
    "@type": "Offer",
    "price": "290",
    "priceCurrency": "RUB"
  }
};
6. Alt теги на всех изображениях:
tsx<Image src="/tarot.jpg" alt="Пример карты Таро с лицом пользователя" />

Производительность
Обязательно:

Используй Next.js <Image> компонент (автооптимизация)
Lazy loading для компонентов ниже fold:

typescript  const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
    loading: () => <LoadingMagic />
  });
```
- Server Components где возможно (меньше JS на клиенте)
- Минимизация client-side JS:
  - `'use client'` только где действительно нужно (формы, анимации)

**Оптимизация:**
- Vercel автоматически минифицирует JS/CSS
- Используй WebP/AVIF форматы для изображений
- Prefetch следующей страницы при hover

---

## 💬 КОММУНИКАЦИЯ (как мы работаем)

### Я не программист, поэтому:

**✅ ТЫ ДЕЛАЕШЬ (Claude Code):**

**1. Объясняешь ТОЛЬКО критичные вещи:**
- Архитектурные решения:
  - "Создаю универсальный AI адаптер — это позволит легко менять провайдеров за 15 минут"
- Важные компромиссы:
  - "Этот подход быстрее, но менее гибкий в будущем. Продолжить?"
- Потенциальные проблемы:
  - "Внимание: Eden AI не поддерживает lip-sync. Нужно использовать Sync.Labs напрямую"

**2. Задаёшь уточняющие вопросы с вариантами:**
```
Уточни: Куда сохранять загруженные фото?
A) Supabase Storage (рекомендую, бесплатно до 1GB)
B) Vercel Blob (платно, но быстрее)
Рекомендую: A
```

**3. Предупреждаешь о важных моментах:**
- "Перед продакшеном не забудь добавить реальные API ключи в Vercel"

---

**❌ ТЫ НЕ ДЕЛАЕШЬ:**

- Не объясняешь каждую строку кода
- Не перегружаешь техническими терминами
- Не спрашиваешь про очевидные вещи ("Какой цвет кнопки?")
- Не даёшь множественный выбор где есть очевидно лучший вариант

---

### Формат ответов

**При создании файла/компонента:**
```
Создаю Header: навигация с логотипом и переключателем темы
[код]
✅ Готово! Проверь: npm run dev
```

**При установке библиотек:**
```
Устанавливаю Framer Motion — для плавных анимаций
Команда: npm install framer-motion
```

**При ошибке:**
```
❌ Ошибка: Не найден модуль 'supabase'
✅ Исправляю: Устанавливаю @supabase/supabase-js
```

**При уточнении:**
```
Уточни: Какой стиль кнопок для CTA?
A) Заполненная (solid) — ярче, привлекает внимание
B) С обводкой (outline) — сдержаннее
Рекомендую: A (для конверсии)
```

---

## 📝 GIT WORKFLOW

### Коммиты (после каждого этапа)

**Формат:** `<тип>: <описание>`

**Примеры:**
- `feat: Initial setup with Next.js 14 and Tailwind`
- `feat: Add layout with Header and Footer`
- `feat: Add Tarot service form with validation`
- `feat: Integrate Supabase database`
- `feat: Add AI text generation service`
- `feat: Add YooKassa payment integration`
- `feat: Complete Tarot end-to-end workflow`
- `fix: Fix mobile responsiveness on forms`
- `style: Update color palette to Violet + Gold`

**Частота:**
- После завершения каждого логического блока
- Минимум 5-10 коммитов в день

**Ветки:**
- Для MVP работаем в `main`
- Если эксперименты — создаём `feature/название`

---

## 🎯 ПРИОРИТЕТЫ РАЗРАБОТКИ

### Скорость MVP (2 дня):

**День 1:**
1. Фронт: Layout + главная + формы всех сервисов
2. БД: Supabase (таблицы users, orders, service_options)
3. Юридические страницы (заглушки)
4. SEO: metadata на всех страницах

**День 2:**
1. AI: Генерация текста (один сервис — Таро)
2. AI: Генерация изображения + FaceSwap (Таро)
3. Платежи: YooKassa интеграция
4. Email: Resend (отправка результата)
5. Полный цикл Таро: форма → AI → оплата → результат → email

---

### Качество важнее "красоты кода"

- Работающий продукт > идеальная архитектура
- Можно использовать готовые решения (Shadcn, библиотеки)
- Рефакторинг потом, после первых продаж

---

### Гибкость (легко расширять)

**Добавление новой опции:**
- Просто INSERT в таблицу `service_options` (без кода)

**Смена AI провайдера:**
- Одна строка в `services/ai/ai-provider.ts`

**Смена платёжки:**
- Одна строка в `services/payment/payment-service.ts`

**Изменение workflow:**
- Правка конфига в `services/workflow/workflows.ts`

**Добавление нового сервиса:**
- Копируешь структуру существующего (Таро/Санта/Песня)
- Меняешь тексты и логику генерации

---

## 🚨 ЧАСТЫЕ ОШИБКИ (НЕ ДОПУСКАЙ)

1. ❌ Генерация AI контента до оплаты → **слив денег**
2. ❌ Хранение API ключей в коде → **утечка данных**
3. ❌ Использование localStorage для критичных данных → **уязвимость**
4. ❌ Отсутствие чекбокса "Согласен с политикой" → **нарушение закона**
5. ❌ Долгие анимации (>500ms) → **тормозит UX**
6. ❌ Забыть отправить email с результатом → **плохой UX**
7. ❌ Не проверять webhook подпись → **уязвимость безопасности**
8. ❌ Не тестировать на мобильных устройствах → **половина юзеров уйдёт**
9. ❌ Фиксированные px размеры → **не адаптивно**
10. ❌ Отсутствие alt тегов на картинках → **плохой SEO**

---

## 🎬 ФИНАЛЬНАЯ ЦЕЛЬ

### MVP готов, когда:

**Фронт:**
- ✅ Красивый сайт со всеми страницами (главная, 3 сервиса, юридические)
- ✅ Переключатель светлой/тёмной темы работает
- ✅ Адаптивность: отлично на всех устройствах (320px - 1920px+)
- ✅ Анимации: плавные, не тормозят

**Функционал:**
- ✅ Один сервис (Таро) работает полностью:
  - Форма → AI текст → показ юзеру → оплата → AI картинка → email
- ✅ Платёжка интегрирована (sandbox режим работает)
- ✅ Email уведомления приходят с правильным оформлением
- ✅ Результат доступен на странице `/order/[id]` для скачивания

**Техническое:**
- ✅ БД: таблицы созданы, связи настроены
- ✅ Универсальные адаптеры: AI и Payment готовы к смене провайдеров
- ✅ SEO: metadata, sitemap, robots.txt
- ✅ Код на GitHub, коммиты осмысленные

**Готовность к продакшену:**
- ✅ Сайт задеплоен на Vercel с доменом
- ✅ .env переменные настроены в Vercel
- ✅ Примеры результатов загружены (для модерации платёжки)
- ✅ Тексты юридических страниц заполнены

---

### После MVP (следующие шаги):

1. **Добавить сервисы "Дед Мороз" и "Песня"** (по той же схеме, 1 день)
2. **Модерация платёжки** (YooKassa, 3-5 дней ожидания)
3. **Первые продажи** (тестирование на реальных пользователях)
4. **Собрать отзывы** (добавить на сайт, Highlights в Instagram)
5. **Масштабировать маркетинг** (больше Reels, платная реклама)
