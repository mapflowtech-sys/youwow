# 💰 Как изменить цену песни

## Быстрый способ (только одно место!)

### Откройте файл:
```
lib/payment/config.ts
```

### Измените значение:
```typescript
// Price in rubles
export const SONG_PRICE = 590; // ← Измените эту цифру
```

### Примеры:
```typescript
// Для тестирования (50 рублей)
export const SONG_PRICE = 50;

// Продакшн цена (590 рублей)
export const SONG_PRICE = 590;

// Акция (390 рублей)
export const SONG_PRICE = 390;
```

## ⚠️ Важно!

После изменения цены:

1. **Локально:**
   - Перезапустите dev сервер: `Ctrl+C` → `npm run dev`
   - Проверьте в модальном окне оплаты

2. **На продакшене:**
   ```bash
   git add lib/payment/config.ts
   git commit -m "chore: Update song price to XXX rubles"
   git push origin main
   ```
   - После деплоя новая цена применится автоматически

## 📝 Где отображается цена:

### ✅ Автоматически обновится:
- Модальное окно оплаты (сумма к оплате)
- API создания платежа
- База данных (поле `amount`)
- Личный кабинет 1plat (сумма платежа)

### ⚠️ Нужно обновить вручную (только для визуала):

Если вы хотите изменить **отображаемую** цену и скидку в модальном окне:

**Файл:** `app/(services)/song/components/PaymentModal.tsx`

Найдите блок "Price Display" (строка ~90):
```tsx
<div className="bg-linear-to-r from-purple-50 to-pink-50 dark:from-gray-800 dark:to-gray-700 rounded-xl p-6 mb-6 text-center">
  <div className="text-sm text-gray-500 dark:text-gray-400 mb-1">
    Стоимость песни
  </div>
  <div className="flex items-center justify-center gap-3 mb-2">
    <span className="text-2xl text-gray-400 dark:text-gray-500 line-through">
      1 190₽  {/* ← Старая цена (зачёркнутая) */}
    </span>
    <span className="bg-red-500 text-white text-xs font-bold px-2 py-1 rounded-md">
      -50%  {/* ← Размер скидки */}
    </span>
  </div>
  <div className="text-5xl font-bold text-purple-600 dark:text-purple-400">
    590₽  {/* ← Текущая цена (отображаемая) */}
  </div>
</div>
```

**Но лучше оставить как есть** - пользователи видят привлекательную цену со скидкой, а реальная цена берётся из `config.ts`

## 🔍 Текущая настройка:

- **Реальная цена для оплаты:** 50₽ (тестовая)
- **Отображаемая цена:** 590₽ (с визуальной скидкой)
- **После теста вернуть на:** 590₽

## 📊 После изменения цены:

1. Проверьте локально модальное окно
2. Сделайте тестовый платёж (если нужно)
3. Задеплойте на продакшн
4. Проверьте в личном кабинете 1plat правильность суммы

---

**Важно:** Цена в `config.ts` - это единственное место, где нужно менять реальную стоимость!
