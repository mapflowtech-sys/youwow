# 📍 Инструкция по обновлению Sitemap

## Когда нужно обновлять sitemap?

### ✅ Обязательно обновить:

1. **Добавлен новый сервис** (новая страница)
   - Открой `app/sitemap.xml/route.ts`
   - Добавь новую запись в массив `staticPages`
   - Пример:
   ```typescript
   {
     loc: `${baseUrl}/new-service`,
     lastmod: '2026-01-15', // текущая дата
     changefreq: 'weekly',
     priority: 0.9,
   }
   ```

2. **Изменилась цена на услугу**
   - Открой `app/sitemap.xml/route.ts`
   - Найди нужную страницу в `staticPages`
   - Обнови поле `lastmod` на текущую дату
   - Пример: `lastmod: '2026-01-15'`

3. **Значительно изменился контент страницы**
   - Обнови `lastmod` для этой страницы

4. **Удалена страница/сервис**
   - Удали соответствующую запись из `staticPages`

### ❌ НЕ нужно обновлять:

- Мелкие правки текста (опечатки)
- Изменения дизайна без изменения контента
- Обновление картинок

## 🚀 Как обновить sitemap (пошагово)

### Шаг 1: Открой файл
```bash
app/sitemap.xml/route.ts
```

### Шаг 2: Найди нужную страницу в массиве `staticPages`

Например, для страницы `/song`:
```typescript
{
  loc: `${baseUrl}/song`,
  lastmod: '2026-01-01', // <-- обнови эту дату
  changefreq: 'weekly',
  priority: 0.9,
}
```

### Шаг 3: Обнови дату `lastmod` на текущую

Формат: `YYYY-MM-DD` (год-месяц-день)

### Шаг 4: Сохрани и задеплой

После коммита и деплоя sitemap автоматически обновится!

## 📊 Параметры sitemap (что они означают)

- **loc** - полный URL страницы
- **lastmod** - дата последнего изменения (YYYY-MM-DD)
- **changefreq** - как часто страница обновляется:
  - `always` - постоянно
  - `hourly` - каждый час
  - `daily` - каждый день
  - `weekly` - еженедельно (для большинства страниц)
  - `monthly` - ежемесячно (для статичных страниц)
  - `yearly` - раз в год
  - `never` - никогда
- **priority** - приоритет страницы (0.0 - 1.0):
  - `1.0` - главная страница
  - `0.9` - основные сервисы
  - `0.7` - дополнительные сервисы
  - `0.3` - юридические страницы

## 🔄 Автоматическое обновление (будущее)

Когда появятся динамические страницы из БД:
1. Раскомментируй функцию `fetchDynamicPages()` в конце файла
2. Подключи её к базе данных (Supabase)
3. Страницы будут добавляться автоматически!

## 🌐 Проверка sitemap

После деплоя проверь:
- Открой в браузере: https://youwow.ru/sitemap.xml
- Проверь в Google Search Console
- Проверь в Яндекс.Вебмастере

## 📝 Примеры изменений

### Пример 1: Добавили новый сервис "Поздравление от Санты"
```typescript
{
  loc: `${baseUrl}/santa-greeting`,
  lastmod: '2026-01-10',
  changefreq: 'weekly',
  priority: 0.8,
}
```

### Пример 2: Изменилась цена на песни (590₽ → 690₽)
```typescript
{
  loc: `${baseUrl}/song`,
  lastmod: '2026-01-10', // обновили дату
  changefreq: 'weekly',
  priority: 0.9,
}
```

### Пример 3: Обновили контент главной страницы
```typescript
{
  loc: baseUrl,
  lastmod: '2026-01-10', // обновили дату
  changefreq: 'weekly',
  priority: 1.0,
}
```

## ⚠️ Важно помнить

1. После каждого изменения делай `git commit` и `git push`
2. После деплоя можно вручную отправить sitemap в поисковики:
   - Google: https://www.google.com/ping?sitemap=https://youwow.ru/sitemap.xml
   - Яндекс: через Вебмастер → Индексация → Sitemap
3. Sitemap кешируется на 1 час - изменения появятся через час
4. Максимум 50 000 URL в одном sitemap
