# Интеграция платёжной системы 1plat

## ✅ Что сделано

Полностью интегрирована платёжная система 1plat для приёма оплаты за создание персональных песен.

### 1. Структура платёжного модуля

Создана гибкая архитектура для работы с платёжными системами:

```
/lib/payment/
  ├── config.ts           # Конфигурация, переключение провайдеров
  ├── types.ts            # Общие типы
  └── providers/
      └── oneplat.ts      # Провайдер 1plat
```

**Преимущества:**
- Легко добавить Free-Kassa или другую систему
- Переключение между провайдерами через одну переменную
- Единый интерфейс для всех платёжных систем

### 2. API Endpoints

#### `/api/payment/create` (POST)
Создаёт заказ и платёж:
- Создаёт запись order в базе данных
- Создаёт платёж в 1plat
- Возвращает URL для перенаправления на оплату

#### `/api/payment/1plat/webhook` (POST)
Обрабатывает уведомления от 1plat:
- Проверяет подпись webhook
- Обновляет статус заказа на "paid"
- Запускает генерацию песни автоматически

### 3. Пользовательский Flow

1. **Пользователь заполняет форму** → Нажимает "Получить готовую песню"
2. **Модальное окно оплаты** → Выбирает способ оплаты (карта/СБП)
3. **Перенаправление на 1plat** → Безопасная оплата 590₽
4. **Success page** → Автоматический редирект (5 сек) на страницу генерации
5. **Генерация песни** → Статус обновляется в реальном времени
6. **Готовая песня** → Скачивание + отправка на email

### 4. Важные файлы

#### Платёжные компоненты:
- `app/(services)/song/components/PaymentModal.tsx` - Модальное окно оплаты
- `app/(services)/song/components/GenerationFlow.tsx` - Управление потоком

#### Страницы результата:
- `app/payment/success/page.tsx` - Успешная оплата
- `app/payment/failure/page.tsx` - Ошибка оплаты

#### Backend:
- `app/api/payment/create/route.ts` - Создание платежа
- `app/api/payment/1plat/webhook/route.ts` - Обработка webhook

### 5. Конфигурация

#### .env.local
```
ONEPLAT_SHOP_ID=1296
ONEPLAT_SECRET=Kogortalove1!
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

#### Цена
Установлена в `lib/payment/config.ts`:
```typescript
export const SONG_PRICE = 145; // 145 рублей
```

### 6. Безопасность

✅ Проверка подписи webhook (SHA256 + MD5)
✅ Верификация merchant ID
✅ Проверка статуса заказа перед генерацией
✅ Защита от повторной обработки

### 7. Настройки в личном кабинете 1plat

**Обязательно настроить:**

1. **Webhook URL**
   ```
   https://yourdomain.com/api/payment/1plat/webhook
   ```

2. **Success URL** (страница успеха)
   ```
   https://yourdomain.com/payment/success
   ```

3. **Failure URL** (страница ошибки)
   ```
   https://yourdomain.com/payment/failure
   ```

4. **2FA** (рекомендуется включить)
   - Защита аккаунта с реальными деньгами
   - Google Authenticator или Authy

### 8. Статусы платежей

**1plat статусы:**
- `-2` - Нет подходящих реквизитов
- `-1` - Черновик (выбор метода)
- `0` - Ожидает оплаты
- `1` - Оплачен, ждёт подтверждения
- `2` - Подтверждён, закрыт

**Генерация запускается:** При status = 1 или 2

### 9. База данных

#### Таблица `orders`
Поля для отслеживания платежей:
- `payment_id` - GUID платежа от 1plat
- `payment_provider` - "1plat"
- `status` - pending → paid → processing → completed

**Где посмотреть информацию об оплатах:**

1. **Supabase Dashboard**
   - Зайдите в Supabase: https://supabase.com/dashboard
   - Выберите ваш проект: tnokozbumscdhxrzgfit
   - Table Editor → orders
   - Фильтруйте по `payment_provider = '1plat'`

2. **SQL запрос в Supabase:**
   ```sql
   SELECT
     id,
     created_at,
     customer_email,
     status,
     payment_id,
     payment_provider,
     amount
   FROM orders
   WHERE payment_provider = '1plat'
   ORDER BY created_at DESC;
   ```

3. **Личный кабинет 1plat**
   - Раздел "Платежи"
   - История всех транзакций
   - Детали каждого платежа

### 10. Тестирование

#### Локально:
```bash
npm run dev
```

Перейдите на `/song` и протестируйте весь flow.

**Важно:** Для тестирования webhooks используйте ngrok или подобный сервис:
```bash
ngrok http 3000
```

Затем обновите Webhook URL в 1plat на ngrok URL.

### 11. Production Deployment

**Checklist перед деплоем:**

- [ ] Обновить `NEXT_PUBLIC_APP_URL` в `.env.local` на production URL
- [ ] Настроить Webhook URL в 1plat на production
- [ ] Настроить Success/Failure URLs в 1plat
- [ ] Проверить, что 1plat верификация пройдена (файл `public/1plat.txt` доступен)
- [ ] Включить 2FA в личном кабинете 1plat
- [ ] Протестировать реальную оплату

### 12. Добавление Free-Kassa (в будущем)

Для переключения на Free-Kassa:

1. Создайте `lib/payment/providers/freekassa.ts`
2. В `lib/payment/config.ts` измените:
   ```typescript
   const providerType: PaymentProviderType = 'freekassa';
   ```
3. Webhook для FK уже есть: `/api/payment/webhook/route.ts`

### 13. Monitoring & Logs

Все логи платежей выводятся с префиксом:
- `[Payment]` - создание платежа
- `[1plat Webhook]` - обработка webhook
- `[1plat]` - операции провайдера

Смотрите в console (dev) или Vercel Logs (production).

### 14. Troubleshooting

**Проблема:** Webhook не приходит
- Проверьте URL в настройках 1plat
- Убедитесь, что сервер доступен из интернета
- Проверьте логи Vercel/сервера

**Проблема:** Генерация не запускается после оплаты
- Проверьте логи webhook
- Убедитесь, что заказ найден в БД по payment_id
- Проверьте, что API `/api/song/process` работает

**Проблема:** Invalid signature
- Проверьте ONEPLAT_SECRET в .env.local
- Убедитесь, что используете правильный ключ из ЛК 1plat

---

## 🎉 Готово к использованию!

Система полностью интегрирована и готова принимать реальные платежи.

Если возникнут вопросы - проверьте логи или напишите в поддержку 1plat.
