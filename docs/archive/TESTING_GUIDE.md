# Инструкция по тестированию нового платёжного флоу

## Что изменилось

### ✅ Основные улучшения:

1. **Виджет ЮKassa вместо редиректа**
   - Пользователь НЕ покидает сайт при оплате
   - Платёжная форма встроена прямо на страницу
   - После оплаты автоматический переход на страницу заказа

2. **Улучшенная страница /order/[id]**
   - Красивые анимации процесса генерации
   - Интересные факты о музыке во время ожидания (10 фактов, меняются каждые 8 секунд)
   - Прогресс-индикатор с шагами: Форма → Оплата → Генерация
   - Кнопки социальных сетей для шеринга (WhatsApp, Telegram, VK, Копировать ссылку)
   - Встроенный аудиоплеер для прослушивания песни

3. **Тестовая страница /test**
   - Упрощённая версия для быстрого тестирования
   - Минимальная форма с обязательными полями
   - Прямая интеграция с виджетом ЮKassa

---

## Как тестировать

### Вариант 1: Тестовая страница `/test` (рекомендуется)

1. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

2. **Откройте тестовую страницу:**
   ```
   http://localhost:3000/test
   ```

3. **Заполните минимальную форму:**
   - О ком песня: `Мой друг Алексей`
   - О чём спеть: `Любит футбол и пиво, душа компании`
   - Жанр: выберите любой
   - Голос: выберите любой
   - Email: ваш email
   - ✅ Согласие с политикой

4. **Нажмите "Перейти к оплате (590₽)"**

5. **Должен появиться виджет ЮKassa:**
   - ✅ Прогресс-бар: Форма ✅ → Оплата 🔵 → Генерация ⚪
   - ✅ Три блока гарантий (зелёный, синий, фиолетовый)
   - ✅ Платёжная форма ЮKassa встроена на странице

6. **Введите тестовые данные карты:**
   ```
   Номер карты: 5555 5555 5555 4477
   Срок: 01/30 (или любая дата больше текущей)
   CVC: 123
   3-D Secure: 123
   ```

7. **После успешной оплаты:**
   - ✅ Показывается анимация "Оплата прошла успешно!"
   - ✅ Автоматический переход на `/order/[id]`

8. **На странице заказа вы увидите:**
   - ✅ Анимированный прогресс: Форма ✅ → Оплата ✅ → Генерация 🔵
   - ✅ Статус "Создаём вашу песню"
   - ✅ Интересные факты о музыке (меняются каждые 8 секунд)
   - ✅ Текст: "Это займёт 3-5 минут. Можете вернуться позже..."

9. **После завершения генерации (~3-5 мин):**
   - ✅ Прогресс: Форма ✅ → Оплата ✅ → Генерация ✅
   - ✅ Статус "Ваша песня готова!"
   - ✅ Встроенный аудиоплеер
   - ✅ Кнопка "Скачать песню"
   - ✅ Кнопки социальных сетей:
     - WhatsApp
     - Telegram
     - VK
     - Скопировать ссылку

---

### Вариант 2: Основная страница `/song`

На данный момент основная страница `/song` **ещё НЕ обновлена** и использует старый флоу с редиректом.

Чтобы протестировать новый флоу на `/song`, нужно:
1. Обновить компонент `GenerationFlow` в `/song/components/`
2. Добавить параметр `useWidget: true` при вызове API

---

## Проверка функциональности

### ✅ Чеклист для тестирования:

#### Шаг 1: Форма
- [ ] Форма корректно валидируется
- [ ] Все обязательные поля отмечены звёздочкой
- [ ] Email проверяется на корректность
- [ ] Чекбокс "Согласие с политикой" обязателен

#### Шаг 2: Виджет оплаты
- [ ] Виджет ЮKassa загружается и отображается
- [ ] Виджет адаптивный (проверить на мобильном)
- [ ] Прогресс-бар показывает текущий шаг
- [ ] Гарантии отображаются корректно
- [ ] Виджет показывает все способы оплаты (Карта, ЮMoney)

#### Шаг 3: Оплата
- [ ] Тестовая карта принимается
- [ ] После успешной оплаты показывается анимация
- [ ] Автоматический переход на `/order/[id]`

#### Шаг 4: Страница заказа (во время генерации)
- [ ] Прогресс-индикатор работает корректно
- [ ] Статус "Создаём вашу песню" отображается
- [ ] Интересные факты меняются каждые 8 секунд
- [ ] Анимации работают плавно
- [ ] Страница автоматически обновляется каждые 5 секунд

#### Шаг 5: Готовая песня
- [ ] Статус меняется на "Ваша песня готова!"
- [ ] Аудиоплеер работает (play/pause)
- [ ] Кнопка "Скачать песню" скачивает файл
- [ ] Кнопки соцсетей работают:
  - [ ] WhatsApp открывает диалог
  - [ ] Telegram открывает диалог
  - [ ] VK открывает окно шаринга
  - [ ] Копировать ссылку - копирует в буфер обмена

---

## Известные особенности

### СБП (Система быстрых платежей)
- ❌ **НЕ доступна в тестовом режиме**
- ✅ Будет доступна на боевом режиме после:
  1. Переключения на реальный магазин
  2. Включения СБП в настройках ЮKassa

### Тестовые данные
- Используйте только тестовые карты от ЮKassa
- В тестовом режиме деньги не списываются
- Email для тестирования можно использовать любой

### ⚠️ Важно: Автоматический запуск генерации

**Исправлено 30.12.2025:**

Генерация теперь запускается **автоматически сразу после успешной оплаты** через виджет, без ожидания webhook от ЮKassa.

**Почему это важно:**
- В локальной разработке webhook от ЮKassa не приходит (localhost недоступен извне)
- На production webhook тоже может задерживаться
- Прямой вызов `/api/song/process` из виджета гарантирует мгновенный старт генерации

**Что это значит для тестирования:**
- ✅ Генерация начнётся сразу после оплаты
- ✅ Не нужно настраивать ngrok или туннели
- ✅ Работает одинаково в dev и production

---

## Откат изменений

Если новый флоу не понравится, можно легко вернуться к старому:

```bash
git checkout main
```

Или если уже находитесь на ветке `feature/payment-widget`:

```bash
# Вернуться к main ветке
git checkout main
```

Старая версия OrderStatusDisplay сохранена в файле:
```
app/order/[id]/OrderStatusDisplayOld.tsx
```

---

## Структура изменённых файлов

### Новые файлы:
- `types/yookassa-widget.d.ts` - TypeScript типы для виджета
- `components/PaymentWidget.tsx` - Компонент виджета ЮKassa
- `app/test/page.tsx` - Тестовая страница
- `YOOKASSA_WIDGET_GUIDE.md` - Документация по виджету
- `TESTING_GUIDE.md` - Эта инструкция

### Изменённые файлы:
- `lib/payment/types.ts` - добавлен `useWidget` и `confirmationToken`
- `lib/payment/providers/yookassa.ts` - поддержка виджета
- `app/api/payment/create/route.ts` - возврат `confirmationToken`
- `app/order/[id]/OrderStatusDisplay.tsx` - улучшенная версия (старая в OrderStatusDisplayOld.tsx)

---

## Вопросы и поддержка

Если возникли вопросы или проблемы при тестировании:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи сервера в терминале
3. Убедитесь, что все переменные окружения настроены:
   - `PAYMENT_PROVIDER=yookassa`
   - `YOOKASSA_SHOP_ID=...`
   - `YOOKASSA_SECRET_KEY=...`

---

**Дата создания:** 30.12.2024
**Версия:** 1.0
**Ветка:** `feature/payment-widget`
