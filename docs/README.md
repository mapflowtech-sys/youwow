# Документация партнёрской системы YouWow

Добро пожаловать в документацию партнёрской программы YouWow!

---

## Инструкции

### 📘 [Инструкция для партнёров](./PARTNER_INSTRUCTIONS.md)
Полное руководство для партнёров по работе с партнёрской программой:
- Как получить партнёрскую ссылку
- Как размещать ссылку на своих ресурсах
- Как отслеживать статистику в личном кабинете
- Как получать выплаты
- Советы по продвижению

**Для кого:** Партнёры, которые хотят зарабатывать с YouWow

---

### 🛠️ [Инструкция для администратора](./ADMIN_INSTRUCTIONS.md)
Полное руководство для администратора по управлению партнёрской системой:
- Создание и редактирование партнёров
- Просмотр статистики и графиков
- Управление статусами (активация, деактивация, архивация)
- Выплата комиссий
- Работа с базой данных Supabase
- **Как правильно общаться с Claude Code для внесения изменений**

**Для кого:** Администраторы YouWow

---

### ✅ [Инструкция по тестированию](./TESTING_INSTRUCTIONS.md)
Подробный сценарий тестирования партнёрской системы:
- 11 основных сценариев тестирования
- Проверка edge cases (граничных случаев)
- Чеклист перед запуском в продакшн
- Что делать при обнаружении багов

**Для кого:** Разработчики и тестировщики

---

## Быстрый старт

### Для партнёров

1. Получите партнёрскую ссылку от администратора
2. Откройте личный кабинет: `https://youwow.ru/partners/ваш-id`
3. Разместите ссылку на своих ресурсах
4. Отслеживайте статистику в личном кабинете

[Читать полную инструкцию →](./PARTNER_INSTRUCTIONS.md)

### Для администраторов

1. Откройте админ-панель: `https://youwow.ru/admin/partners`
2. Создайте партнёра (кнопка "Создать партнёра")
3. Отслеживайте статистику и выплачивайте комиссии

[Читать полную инструкцию →](./ADMIN_INSTRUCTIONS.md)

---

## Архитектура системы

### Основные компоненты

```
┌─────────────────────────────────────────────────────────┐
│                    Партнёрская система                   │
└─────────────────────────────────────────────────────────┘

1. Партнёрская ссылка
   └─> https://youwow.ru/song?partner=ID

2. Middleware (отслеживание кликов)
   └─> Сохраняет cookie: partner_data
   └─> Записывает клик в partner_clicks

3. Оплата заказа
   └─> Проверяет cookie: partner_data
   └─> Записывает конверсию в partner_conversions

4. Админ-панель
   └─> Управление партнёрами
   └─> Просмотр статистики
   └─> Выплата комиссий

5. Публичный дашборд
   └─> Личный кабинет партнёра
   └─> Статистика и графики
```

### База данных (Supabase)

**Таблицы:**

1. **partners** — список партнёров
   - `id` (PK)
   - `name`, `website`, `payment_info`, `notes`
   - `commission_rate` (комиссия в рублях)
   - `status` (active/inactive/archived)

2. **partner_clicks** — клики по партнёрским ссылкам
   - `id` (PK)
   - `partner_id` (FK → partners)
   - `session_id` (UUID для связи с конверсией)
   - `clicked_at`, `ip_address`, `user_agent`, `referrer`

3. **partner_conversions** — конверсии (покупки)
   - `id` (PK)
   - `partner_id` (FK → partners)
   - `session_id` (связь с кликом)
   - `order_id` (FK → orders)
   - `amount`, `commission`
   - `is_paid_out` (выплачена ли комиссия)

4. **partner_payouts** — история выплат
   - `id` (PK)
   - `partner_id` (FK → partners)
   - `amount`, `conversions_count`
   - `period_start`, `period_end`
   - `payment_method`, `notes`

### API Endpoints

**Публичные:**
- `GET /api/partners/[id]/stats` — статистика партнёра
- `GET /api/partners/[id]/chart-data?days=30` — данные для графиков

**Для админов:**
- `GET /api/admin/partners/list` — список всех партнёров
- `POST /api/admin/partners/create` — создать партнёра
- `PATCH /api/admin/partners/[id]` — обновить партнёра
- `PATCH /api/admin/partners/[id]/status` — изменить статус
- `GET /api/admin/partners/[id]/stats` — детальная статистика
- `GET /api/admin/partners/[id]/conversions?limit=20` — конверсии партнёра
- `POST /api/admin/partners/[id]/payout` — создать выплату

---

## Основные концепции

### Жизненный цикл партнёрской конверсии

```
1. Клик по ссылке
   ↓
   Middleware сохраняет cookie (partner_data)
   Записывает клик в partner_clicks

2. Пользователь создаёт песню
   ↓
   Переходит на оплату

3. Оплата успешна
   ↓
   Система проверяет cookie
   Если есть partner_data → создаёт partner_conversion

4. Конверсия создана
   ↓
   is_paid_out = false
   Отображается в админ-панели как "К выплате"

5. Администратор делает выплату
   ↓
   is_paid_out = true
   Создаётся запись в partner_payouts
   Отображается как "Выплачено"
```

### Статусы партнёра

- **active (Активен)** — партнёрская ссылка работает, клики отслеживаются
- **inactive (Неактивен)** — партнёрская ссылка НЕ работает, клики не отслеживаются
- **archived (В архиве)** — скрыт из основного списка, но ссылка продолжает работать

### Cookie отслеживания

**Название:** `partner_data`

**Содержимое:**
```json
{
  "partnerId": "test-partner1",
  "sessionId": "uuid-v4",
  "clickedAt": "2026-01-06T12:00:00Z",
  "landingPage": "/song"
}
```

**Срок жизни:** 30 дней

---

## Полезные ссылки

- **Админ-панель:** https://youwow.ru/admin/partners
- **Supabase Dashboard:** https://supabase.com/dashboard
- **Vercel Dashboard:** https://vercel.com/dashboard
- **GitHub репозиторий:** https://github.com/mapflowtech-sys/youwow

---

## Техническая поддержка

### Для партнёров

Если у вас возникли вопросы:
- Проверьте статистику в личном кабинете
- Напишите администратору (контакты при регистрации)

### Для администраторов

Если нужна помощь с системой:
- Проверьте [инструкцию администратора](./ADMIN_INSTRUCTIONS.md)
- Обратитесь к разработчику
- Используйте Claude Code для внесения изменений

### Как правильно общаться с Claude Code

См. раздел "Как общаться с Claude Code для изменений" в [инструкции администратора](./ADMIN_INSTRUCTIONS.md#как-общаться-с-claude-code-для-изменений)

---

## История изменений

### Версия 1.0 (январь 2026)
- Базовая партнёрская система
- Отслеживание кликов и конверсий
- Админ-панель для управления партнёрами
- Публичный дашборд для партнёров
- Система выплат
- Статусы партнёров (active/inactive/archived)
- Графики статистики по дням

---

**Последнее обновление:** 6 января 2026
